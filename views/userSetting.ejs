<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Solaready Settings</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        /* SolarTech Dark Theme CSS Variables */
        :root {
            --bg-dark: #121212;
            --bg-medium: #1e1e1e;
            --bg-light: #2c2c2c;
            --text-light: #e0e0e0;
            --text-secondary: #a0a0a0;
            --accent-green: #00ff80;
            --accent-blue: #00d4ff;
            --gradient-primary: linear-gradient(135deg, var(--accent-green), var(--accent-blue));
            --shadow-glow: 0 0 20px rgba(0, 255, 128, 0.2), 0 0 40px rgba(0, 212, 255, 0.1);
            --border-radius: 12px;
            --transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        /* Global styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg-dark);
            color: var(--text-light);
            line-height: 1.6;
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Dashboard Layout */
        .dashboard-layout {
            display: flex;
            flex-grow: 1;
            padding-top: 64px; /* Space for the fixed header */
        }

        /* Header */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 64px;
            background: rgba(18, 18, 18, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--bg-light);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 24px;
        }

        .header-left, .header-right {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .logo {
            font-family: 'Orbitron', monospace;
            font-size: 24px;
            font-weight: 700;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 5px rgba(0, 255, 128, 0.5);
        }
        
        .logo-icon {
            color: var(--accent-green);
        }

        .header-buttons {
            display: flex;
            gap: 8px;
        }

        .header-btn {
            background: none;
            border: none;
            color: var(--text-light);
            cursor: pointer;
            padding: 8px;
            border-radius: 50%;
            transition: var(--transition);
        }

        .header-btn:hover {
            background: var(--bg-light);
            color: var(--accent-green);
        }
        
        .notification-badge {
            position: absolute;
            top: -4px;
            right: -4px;
            background: #ef4444;
            color: white;
            border-radius: 50%;
            padding: 2px 6px;
            font-size: 10px;
            font-weight: 600;
        }

        /* Sidebar */
        .sidebar {
            width: 256px;
            background: var(--bg-medium);
            border-right: 1px solid var(--bg-light);
            padding: 24px 0;
            position: fixed;
            height: calc(100vh - 64px);
            top: 64px;
            z-index: 999;
            transition: transform 0.3s ease-in-out;
            transform: translateX(0);
        }
        
        .sidebar-content {
            padding: 0 16px;
            height: 100%;
            overflow-y: auto;
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 24px;
            padding: 12px;
            background: var(--bg-light);
            border-radius: var(--border-radius);
            border: 1px solid rgba(0, 255, 128, 0.1);
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background: var(--accent-green);
            color: var(--bg-dark);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        .user-info .name {
            font-weight: 600;
            color: var(--text-light);
        }

        .user-info .email {
            font-size: 12px;
            color: var(--text-secondary);
        }

        .sidebar-nav-group {
            margin-bottom: 24px;
        }
        
        .sidebar-nav-group h2 {
            font-size: 12px;
            color: var(--text-secondary);
            font-weight: 600;
            padding: 8px 16px;
            margin-bottom: 8px;
            letter-spacing: 0.1em;
        }

        .sidebar-nav-link {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 10px 16px;
            border-radius: 8px;
            text-decoration: none;
            color: var(--text-secondary);
            transition: var(--transition);
        }
        
        .sidebar-nav-link:hover {
            background: rgba(0, 255, 128, 0.1);
            color: var(--accent-green);
        }

        .sidebar-nav-link.active {
            background: rgba(0, 255, 128, 0.15);
            color: var(--accent-green);
            font-weight: 600;
            border-left: 2px solid var(--accent-green);
        }
        
        .sidebar-nav-link svg {
            width: 16px;
            height: 16px;
        }
        
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }
        }
        
        .hamburger-menu {
            display: none;
        }
        
        @media (max-width: 768px) {
            .hamburger-menu {
                display: block;
            }
        }

        /* Main Content */
        .main-content {
            flex-grow: 1;
            padding: 24px;
            margin-left: 256px; /* Offset for sidebar */
            transition: margin-left 0.3s ease-in-out;
        }
        
        @media (max-width: 768px) {
            .main-content {
                margin-left: 0;
            }
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 32px;
        }

        .dashboard-header h1 {
            font-size: 32px;
            color: var(--text-light);
            background: none;
            -webkit-text-fill-color: var(--text-light);
            text-shadow: none;
        }

        .dashboard-header p {
            color: var(--text-secondary);
        }
        
        /* Settings-specific styles */
        .settings-container {
            max-width: 900px;
            margin: 0 auto;
            display: grid;
            gap: 24px;
            grid-template-columns: 1fr;
        }

        @media (min-width: 768px) {
            .settings-container {
                grid-template-columns: 2fr 1fr;
            }
        }

        .card {
            background: rgba(30, 30, 30, 0.6);
            backdrop-filter: blur(5px);
            border: 1px solid var(--bg-light);
            border-radius: var(--border-radius);
            padding: 24px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: var(--transition);
        }

        .card-header {
            display: flex;
            flex-direction: column;
            margin-bottom: 24px;
        }

        .card-header .title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text-light);
        }

        .card-header .description {
            font-size: 14px;
            color: var(--text-secondary);
        }
        
        .form-group {
            margin-bottom: 24px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--text-light);
        }

        .form-group input {
            width: 100%;
            padding: 12px;
            background: var(--bg-dark);
            border: 1px solid var(--bg-light);
            border-radius: 8px;
            color: var(--text-light);
            font-size: 1rem;
            transition: var(--transition);
        }
        
        .form-group input:focus {
            outline: none;
            border-color: var(--accent-green);
            box-shadow: 0 0 8px rgba(0, 255, 128, 0.2);
        }
        
        .action-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
        }

        .action-item .info h3 {
            margin: 0;
            color: var(--text-light);
        }

        .action-item .info p {
            margin: 4px 0 0;
            font-size: 14px;
            color: var(--text-secondary);
        }

        .action-btn {
            background: var(--bg-dark);
            border: 1px solid var(--bg-light);
            color: var(--text-light);
            padding: 8px 16px;
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition);
        }

        .action-btn:hover {
            border-color: var(--accent-green);
            color: var(--accent-green);
        }
        
        .form-divider {
            height: 1px;
            background-color: var(--bg-light);
            margin: 24px 0;
        }
        
        .form-actions {
            display: flex;
            justify-content: flex-end;
            margin-top: 24px;
        }

        .form-actions button {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 20px;
            border-radius: 50px;
            background: var(--gradient-primary);
            color: var(--bg-dark);
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .form-actions button:hover {
            box-shadow: var(--shadow-glow);
        }
        
        /* Toggle switch for notifications */
        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 44px;
            height: 24px;
        }
        
        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .toggle-slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #333;
            transition: .4s;
            border-radius: 24px;
        }

        .toggle-slider:before {
            position: absolute;
            content: "";
            height: 16px;
            width: 16px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked + .toggle-slider {
            background-color: var(--accent-green);
        }

        input:checked + .toggle-slider:before {
            transform: translateX(20px);
        }

        /* Footer */
        footer {
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 56px;
            background: rgba(18, 18, 18, 0.8);
            backdrop-filter: blur(10px);
            border-top: 1px solid var(--bg-light);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 24px;
        }
        
        footer .footer-links a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.3s ease;
            margin-left: 16px;
        }
        
        footer .footer-links a:hover {
            color: var(--text-light);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }
            .main-content {
                margin-left: 0;
            }
            .hamburger-menu {
                display: block;
            }
            .dashboard-layout.show-sidebar .sidebar {
                transform: translateX(0);
            }
            .dashboard-layout.show-sidebar .main-content {
                margin-left: 256px;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-layout">
        <!-- Header -->
        <header>
            <div class="header-left">
                <div class="hamburger-menu">
                    <button class="header-btn" onclick="toggleSidebar()">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="4" x2="20" y1="12" y2="12"></line>
                            <line x1="4" x2="20" y1="6" y2="6"></line>
                            <line x1="4" x2="20" y1="18" y2="18"></line>
                        </svg>
                    </button>
                </div>
                <div class="logo">Solaready</div>
            </div>
            <div class="header-right">
                <button class="header-btn" title="Notifications">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9"></path>
                        <path d="M10.3 21a1.94 1.94 0 0 0 3.4 0"></path>
                    </svg>
                    <span class="notification-badge">3</span>
                </button>
                <button class="header-btn" title="Log out">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                        <polyline points="16 17 21 12 16 7"></polyline>
                        <line x1="21" x2="9" y1="12" y2="12"></line>
                    </svg>
                </button>
            </div>
        </header>

        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-content">
                <div class="user-profile">
                    <div class="user-avatar"><%= user.name.toString()[0] %></div>
                    <div class="user-info">
                        <p class="name"> <%= user.name  %></p>
                        <p class="email"><%= user.email  %></p>
                    </div>
                </div>

                <div class="sidebar-nav-group">
                    <h2>GENERAL</h2>
                    <a href="http://localhost:8080/dashboard/<%= user._id %>" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M15 21v-8a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v8"></path>
                            <path d="M3 10a2 2 0 0 1 .709-1.528l7-5.999a2 2 0 0 1 2.582 0l7 5.999A2 2 0 0 1 21 10v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                        </svg>
                        <span>Dashboard</span>
                    </a>
                    <a href="http://localhost:8080/user/<%= user._id %>/profile" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
                            <circle cx="12" cy="7" r="4"></circle>
                        </svg>
                        <span>Profile</span>
                    </a>
                    <a href="http://localhost:8080/user/<%= user._id %>/notification" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9"></path>
                            <path d="M10.3 21a1.94 1.94 0 0 0 3.4 0"></path>
                        </svg>
                        <span>Notifications</span>
                    </a>
                </div>

                <div class="sidebar-nav-group">
                    <h2>SUPPORT</h2>
                    <a href="http://localhost:8080/user/<%= user._id %>/tickets" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M2 9a3 3 0 0 1 0 6v2a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-2a3 3 0 0 1 0-6V7a2 2 0 0 0-2-2H4a2 2 0 0 0-2 2Z"></path>
                            <path d="M13 5v2"></path><path d="M13 17v2"></path><path d="M13 11v2"></path>
                        </svg>
                        <span>Tickets</span>
                    </a>
                    <a href="http://localhost:8080/user/<%= user._id %>/livechat" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
                        </svg>
                        <span>Live Chat</span>
                    </a>
                    <a href="http://localhost:8080/user/<%= user._id %>/knowledge" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M12 17h.01"></path>
                            <path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7z"></path>
                            <path d="M9.1 9a3 3 0 0 1 5.82 1c0 2-3 3-3 3"></path>
                        </svg>
                        <span>Knowledge Base</span>
                    </a>
                </div>

                <div class="sidebar-nav-group">
                    <h2>BILLING</h2>
                    <a href="http://localhost:8080/user/<%= user._id %>/payment" class="sidebar-nav-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect width="20" height="14" x="2" y="5" rx="2"></rect>
                            <line x1="2" x2="22" y1="10" y2="10"></line>
                        </svg>
                        <span>Payments</span>
                    </a>
                </div>

                <div class="sidebar-nav-group">
                    <h2>ACCOUNT</h2>
                    <a href="http://localhost:8080/user/<%= user._id %>/settings" class="sidebar-nav-link active">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"></path>
                            <circle cx="12" cy="12" r="3"></circle>
                        </svg>
                        <span>Settings</span>
                    </a>
                </div>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <div class="dashboard-header">
                <div>
                    <h1>Settings</h1>
                    <p>Manage your account information and preferences.</p>
                </div>
            </div>

            <div class="settings-container">
                <!-- Main Settings Cards -->
                <div class="card-group">
                    <!-- Account Information Card -->
                    <div class="card">
                        <div class="card-header">
                            <div class="title">Account Information</div>
                            <div class="description">Update your personal details.</div>
                        </div>
                        <form>
                            <div class="form-group">
                                <label for="name">Full Name</label>
                                <input type="text" id="name" name="name" value="<%= user.name  %>">
                            </div>
                            <div class="form-group">
                                <label for="email">Email Address</label>
                                <input type="email" id="email" name="email" value="<%= user.email  %>">
                            </div>
                            <div class="form-actions">
                                <button type="submit">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                        <path d="M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z"></path>
                                        <path d="M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7"></path>
                                        <path d="M7 3v4a1 1 0 0 0 1 1h7"></path>
                                    </svg>
                                    Save Changes
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- Security Settings Card -->
                    <div class="card" style="margin-top: 24px;">
                        <div class="card-header">
                            <div class="title">Security Settings</div>
                            <div class="description">Manage your account security.</div>
                        </div>
                        <div class="action-item">
                            <div class="info">
                                <h3>Password</h3>
                                <p>Last changed 2 months ago.</p>
                            </div>
                            <button class="action-btn">Change Password</button>
                        </div>
                        <div class="form-divider"></div>
                        <div class="action-item">
                            <div class="info">
                                <h3>Two-Factor Authentication</h3>
                                <p>Disabled</p>
                            </div>
                            <button class="action-btn">Enable</button>
                        </div>
                    </div>
                </div>
                
                <!-- Secondary Settings Cards -->
                <div>
                    <!-- Notification Preferences Card -->
                    <div class="card">
                        <div class="card-header">
                            <div class="title">Notification Preferences</div>
                            <div class="description">Select the alerts you want to receive.</div>
                        </div>
                        <div class="action-item">
                            <div class="info">
                                <h3>Email Notifications</h3>
                                <p>Receive alerts for important updates.</p>
                            </div>
                            <label class="toggle-switch">
                                <input type="checkbox" checked>
                                <span class="toggle-slider"></span>
                            </label>
                        </div>
                        <div class="form-divider"></div>
                        <div class="action-item">
                            <div class="info">
                                <h3>New Ticket Updates</h3>
                                <p>Be notified when a new ticket is created.</p>
                            </div>
                            <label class="toggle-switch">
                                <input type="checkbox" checked>
                                <span class="toggle-slider"></span>
                            </label>
                        </div>
                    </div>
                    
                    <!-- Billing & Subscription Card -->
                    <div class="card" style="margin-top: 24px;">
                        <div class="card-header">
                            <div class="title">Billing & Subscription</div>
                            <div class="description">View your plan details and payment history.</div>
                        </div>
                        <div class="action-item">
                            <div class="info">
                                <h3>Current Plan</h3>
                                <p>Basic Plan - $9.99/month</p>
                            </div>
                            <a href="payments.html" class="action-btn">View Payments</a>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Footer -->
    <footer>
        <p>© 2025 Solaready. All rights reserved.</p>
        <div class="footer-links">
            <a href="#">Privacy Policy</a>
            <a href="#">Terms of Service</a>
        </div>
    </footer>

    <script>
        function toggleSidebar() {
            const layout = document.querySelector('.dashboard-layout');
            layout.classList.toggle('show-sidebar');
        }
    </script>
</body>
</html>
